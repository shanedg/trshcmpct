{
  "name": "@trshcmpctr/scaffold",
  "version": "1.0.0",
  "description": "Quickly generate new package boilerplate.",
  "main": "lib/index.js",
  "files": ["lib"],
  "type": "module",
  "scripts": {
    "asdf": "plop --plopfile lib/index.js project -- --name asdf --path asdf",
    "build": "echo fake build || true",
    "clean": "rimraf *.build.log",
    "lint:md": "markdownlint-cli2-config 'node_modules/@trshcmpctr/markdownlint-config/.markdownlint-cli2.jsonc' '**/*.md' '#node_modules'",
    "lint": "eslint --ext .cjs,.js --max-warnings=0 --fix --cache --cache-location node_modules/.cache/eslint-cache/ .",
    "test": "true"
  },
  "devDependencies": {
    "@trshcmpctr/eslint-config": "workspace:*",
    "@trshcmpctr/markdownlint-config": "workspace:*",
    "eslint-plugin-eslint-comments": "~3.2.0",
    "eslint-plugin-import": "~2.25.4",
    "eslint-plugin-node": "~11.1.0",
    "eslint": "^8.7.0",
    "markdownlint-cli2": "~0.4.0",
    "plop": "~3.0.5",
    "rimraf": "~3.0.2"
  },
  "peerDependencies": {
    "plop": "~3.0.5"
  }
}
